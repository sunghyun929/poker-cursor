# Environment Variables for Poker Game
NODE_ENV=production
PORT=10000

# Optional: Database configuration (if using PostgreSQL instead of in-memory storage)
# DATABASE_URL=postgresql://username:password@hostname:port/database_name